//remember to inject all dependecies that you will use
module.exports = function () {

   var context = this;

   this.getAllEnterprises = function(){
	   //declare and instantiate angular promise
      //create req object if exists
	   //execute gapi method
	   //resolve promise
	   //return angular instance.promise
   }

   this.findByFilter = function(){
   }

   this.getEnterpriseTypes = function(){
   }

   this.addEnterprise = function(){
   };

	this.EmployeesRangeEnum = {
	    //create Enum according EnterpriseEmployeeRangeEnum
	}
};
